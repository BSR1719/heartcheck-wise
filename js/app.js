const q=s=>document.querySelector(s),val=id=>q('#'+id)?.value??'',num=id=>val(id)===''?null:Number(val(id));
function riskBand(r){if(r==null)return{label:'ข้อมูลไม่ครบ',cls:'neutral'};if(r<3)return{label:'Low risk',cls:'low'};if(r<5)return{label:'Borderline risk',cls:'borderline'};if(r<10)return{label:'Intermediate risk',cls:'intermediate'};return{label:'High risk',cls:'high'}}
const pct=v=>v==null?'—':v.toFixed(1)+'%',show=id=>{q(id).hidden=false;q(id).scrollIntoView({behavior:'smooth',block:'start'})};
q('#riskForm').addEventListener('submit',e=>{e.preventDefault();q('#errors').innerHTML='';const emergency=[...document.querySelectorAll('input[name="redflag"]:checked')].length>0;if(emergency){q('#result').innerHTML='<div class="alert danger"><h2>ควรได้รับการประเมินเร่งด่วน</h2><p>อาการที่เลือกอาจต้องได้รับการตรวจโดยแพทย์ทันที กรุณาอย่ารอผลจากเครื่องมือนี้</p></div>';return show('#result')}const established=[...document.querySelectorAll('input[name="ascvdHistory"]:checked')].length>0;if(established){q('#result').innerHTML='<div class="alert warning"><h2>เครื่องมือนี้ไม่เหมาะสำหรับ Secondary Prevention</h2><p>คุณมีประวัติโรคหัวใจหรือหลอดเลือดแล้ว จึงไม่ควรใช้ PREVENT primary-prevention score เป็นตัวตัดสินหลัก ควรประเมินร่วมกับแพทย์</p></div>';return show('#result')}
const input={sex:num('sex'),age:num('age'),sbp:num('sbp'),dm:num('dm'),smoking:num('smoking'),egfr:num('egfr'),bptreat:num('bptreat'),tc:num('tc'),hdl:num('hdl'),statin:num('statin'),bmi:num('bmi')},out=PREVENT.baseRisk(input);if(!out.ok){q('#errors').innerHTML=`<div class="alert danger"><strong>ตรวจสอบข้อมูล:</strong><ul>${out.errors.map(x=>`<li>${x}</li>`).join('')}</ul></div>`;return}const r=out.result,band=riskBand(r.ascvd10),ldl=num('ldl'),dbp=num('dbp'),overrides=[];if(ldl!=null&&ldl>=190)overrides.push('LDL-C ≥190 mg/dL: ต้องเข้าสู่ clinical override pathway ไม่ควรใช้ risk ต่ำเพื่อสร้างความมั่นใจเกินจริง');if(input.sbp>=180||(dbp!=null&&dbp>=120))overrides.push('ความดันสูงมาก: ควรวัดซ้ำอย่างถูกต้องและประเมินอาการเร่งด่วน');const factors=[];if(input.smoking===1)factors.push('สูบบุหรี่');if(input.sbp>=130)factors.push('SBP ≥130 mmHg');if(ldl!=null&&ldl>=160)factors.push('LDL-C ≥160 mg/dL');if(input.bmi!=null&&input.bmi>=25)factors.push('BMI ≥25 kg/m² — ควรประเมินภาวะน้ำหนักเกิน/อ้วนตามเกณฑ์เอเชียร่วมกับรอบเอวและโรคร่วม');if(input.dm===1)factors.push('เบาหวาน');if(input.egfr<60)factors.push('eGFR <60');
q('#result').innerHTML=`<div class="result-head ${band.cls}"><span class="eyebrow">PREVENT-ASCVD 10-year risk</span><div class="risk-number">${pct(r.ascvd10)}</div><div class="risk-band">${band.label}</div></div>${overrides.length?`<div class="alert warning"><strong>Clinical override</strong><ul>${overrides.map(x=>`<li>${x}</li>`).join('')}</ul></div>`:''}<div class="metric-grid"><div class="metric"><span>ASCVD 10 ปี</span><strong>${pct(r.ascvd10)}</strong></div><div class="metric"><span>CVD 10 ปี</span><strong>${pct(r.cvd10)}</strong></div><div class="metric"><span>HF 10 ปี</span><strong>${pct(r.hf10)}</strong></div><div class="metric"><span>ASCVD 30 ปี</span><strong>${pct(r.ascvd30)}</strong></div><div class="metric"><span>CVD 30 ปี</span><strong>${pct(r.cvd30)}</strong></div><div class="metric"><span>HF 30 ปี</span><strong>${pct(r.hf30)}</strong></div></div><div class="result-section"><h3>ปัจจัยที่ควรให้ความสำคัญ</h3><p>${factors.length?factors.join(' • '):'ยังไม่พบปัจจัยเด่นจากข้อมูลที่กรอก'}</p></div><div class="result-section"><h3>การแปลผล</h3><p>${band.cls==='low'?'ความเสี่ยง 10 ปีอยู่ในระดับต่ำ แต่ควรดูปัจจัยเสี่ยงระยะยาวและผล 30 ปีในผู้ที่อายุไม่เกิน 59 ปีด้วย':band.cls==='borderline'?'ควรทบทวน risk enhancers และหารือแนวทางป้องกันกับบุคลากรทางการแพทย์':band.cls==='intermediate'?'ควรประเมิน preventive strategy อย่างเป็นระบบ รวมถึงการควบคุมไขมัน ความดัน เบาหวาน และอาจพิจารณา CAC ในรายที่การตัดสินใจยังไม่ชัดเจน':'ควรได้รับการประเมินปัจจัยเสี่ยงและวางแผนป้องกันโรคหัวใจและหลอดเลือดกับแพทย์'}</p></div><div class="result-section small"><strong>Prototype status:</strong> JavaScript port จาก AHA PREVENT base equation v1.0.0. ก่อนใช้ทางคลินิกจริง ต้อง regression-test ตัวเลขทุก boundary case เทียบกับ official AHA R package.</div>`;show('#result')});
q('#resetBtn').addEventListener('click',()=>{q('#riskForm').reset();q('#result').hidden=true;q('#errors').innerHTML='';window.scrollTo({top:0,behavior:'smooth'})});
